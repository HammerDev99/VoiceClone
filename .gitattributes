# Normaliza finales de línea a LF en el repositorio (evita avisos en Windows).
* text=auto eol=lf

# Archivos binarios (no convertir).
*.m4a binary
*.mp3 binary
*.wav binary
*.png binary
*.jpg binary
