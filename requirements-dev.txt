# Dependencias de desarrollo (tests, linting, type-checking)
# Instalar con:  pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.3.0
pytest-cov>=5.0.0
hypothesis>=6.110.0     # Property-based testing (PBT)
ruff>=0.8.0             # Linter + formateador
mypy>=1.13.0            # Type checking estatico
